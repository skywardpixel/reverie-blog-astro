---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout
  title="404 - Page Not Found | Skyward Blog"
  description="The page you're looking for doesn't exist. Explore our other content."
>
  <section class="error-page">
    <div class="container">
      <div class="error-content">
        <div class="error-visual">
          <div class="floating-glass glass-1"></div>
          <div class="floating-glass glass-2"></div>
          <div class="floating-glass glass-3"></div>
          <div class="error-number">
            <span class="number-4">4</span>
            <span class="number-0">0</span>
            <span class="number-4-end">4</span>
          </div>
        </div>

        <div class="error-message">
          <h1 class="error-title">Page Not Found</h1>
          <p class="error-description">
            The page you're looking for seems to have drifted away into the digital ether.
            Let's help you find your way back to solid ground.
          </p>

          <div class="error-actions">
            <a href="/" class="btn btn-primary">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"></path>
                <polyline points="9 22 9 12 15 12 15 22"></polyline>
              </svg>
              <span>Go Home</span>
            </a>
            <a href="/blog" class="btn btn-secondary">
              <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"></path>
                <path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"></path>
                <line x1="12" y1="6" x2="16" y2="6"></line>
                <line x1="12" y1="10" x2="16" y2="10"></line>
              </svg>
              <span>Browse Articles</span>
            </a>
          </div>

          <div class="helpful-links">
            <h3>Maybe you're looking for:</h3>
            <nav class="quick-links">
              <a href="/blog" class="quick-link">Latest Articles</a>
              <a href="/about" class="quick-link">About</a>
              <a href="/" class="quick-link">Home</a>
            </nav>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .error-page {
    min-height: calc(100vh - 5rem);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 4rem 0;
    position: relative;
    overflow: hidden;
  }

  .error-page::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: radial-gradient(
      circle at center,
      rgba(124, 152, 133, 0.02) 0%,
      transparent 60%
    );
    animation: gentle-pulse 12s ease-in-out infinite;
  }

  @keyframes gentle-pulse {
    0%, 100% {
      opacity: 0.5;
      transform: scale(1);
    }
    50% {
      opacity: 1;
      transform: scale(1.05);
    }
  }

  .error-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    max-width: 1000px;
    position: relative;
    z-index: 2;
  }

  .error-visual {
    position: relative;
    height: 400px;
    display: flex;
    align-items: center;
    justify-content: center;
    perspective: 1000px;
  }

  .floating-glass {
    position: absolute;
    background: var(--glass-bg);
    backdrop-filter: blur(var(--blur-md)) saturate(120%);
    -webkit-backdrop-filter: blur(var(--blur-md)) saturate(120%);
    border: 1px solid var(--glass-border);
    border-radius: var(--radius-lg);
    box-shadow: var(--glass-shadow);
    opacity: 0.6;
  }

  .glass-1 {
    width: 120px;
    height: 80px;
    top: 10%;
    left: -10%;
    transform: rotate(-15deg) translateZ(30px);
    animation: float-gentle-1 18s ease-in-out infinite;
    background: linear-gradient(135deg, rgba(124, 152, 133, 0.03), rgba(164, 184, 168, 0.02));
  }

  .glass-2 {
    width: 100px;
    height: 100px;
    top: 60%;
    right: -15%;
    transform: rotate(20deg) translateZ(60px);
    animation: float-gentle-2 20s ease-in-out infinite;
    background: linear-gradient(135deg, rgba(164, 184, 168, 0.03), rgba(124, 152, 133, 0.02));
  }

  .glass-3 {
    width: 80px;
    height: 60px;
    bottom: 15%;
    left: 15%;
    transform: rotate(8deg) translateZ(45px);
    animation: float-gentle-3 16s ease-in-out infinite;
    background: linear-gradient(135deg, rgba(124, 152, 133, 0.025), rgba(164, 184, 168, 0.015));
  }

  @keyframes float-gentle-1 {
    0%, 100% {
      transform: rotate(-15deg) translateZ(30px) translateY(0px);
    }
    50% {
      transform: rotate(-12deg) translateZ(35px) translateY(-15px);
    }
  }

  @keyframes float-gentle-2 {
    0%, 100% {
      transform: rotate(20deg) translateZ(60px) translateY(0px);
    }
    50% {
      transform: rotate(18deg) translateZ(65px) translateY(-20px);
    }
  }

  @keyframes float-gentle-3 {
    0%, 100% {
      transform: rotate(8deg) translateZ(45px) translateY(0px);
    }
    50% {
      transform: rotate(10deg) translateZ(50px) translateY(-10px);
    }
  }

  .error-number {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    z-index: 10;
    position: relative;
  }

  .error-number span {
    font-family: var(--font-display);
    font-size: clamp(4rem, 12vw, 8rem);
    font-weight: 300;
    line-height: 1;
    text-align: center;
    letter-spacing: -0.05em;
  }

  .number-4,
  .number-4-end {
    background: linear-gradient(135deg, var(--color-accent) 0%, var(--color-accent-light) 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    animation: number-glow 4s ease-in-out infinite;
  }

  .number-0 {
    background: linear-gradient(135deg, var(--color-text) 0%, var(--color-text-secondary) 100%);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    opacity: 0.3;
    animation: number-fade 4s ease-in-out infinite;
  }

  @keyframes number-glow {
    0%, 100% {
      filter: brightness(1);
    }
    50% {
      filter: brightness(1.2);
    }
  }

  @keyframes number-fade {
    0%, 100% {
      opacity: 0.3;
    }
    50% {
      opacity: 0.6;
    }
  }

  .error-message {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .error-title {
    font-family: var(--font-display);
    font-size: clamp(2.5rem, 6vw, 3.5rem);
    font-weight: 500;
    line-height: 1.2;
    letter-spacing: -0.025em;
    color: var(--color-text);
    margin: 0;
  }

  .error-description {
    font-family: var(--font-serif);
    font-size: 1.25rem;
    line-height: 1.7;
    color: var(--color-text-secondary);
    margin: 0;
    max-width: 500px;
  }

  .error-actions {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2rem;
    border-radius: var(--radius-md);
    font-family: var(--font-serif);
    font-weight: 500;
    font-size: 1.05rem;
    text-decoration: none;
    transition: all var(--transition-base);
    position: relative;
    overflow: hidden;
  }

  .btn::before {
    content: "";
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
    transition: left var(--transition-base);
  }

  .btn:hover::before {
    left: 100%;
  }

  .btn-primary {
    background: linear-gradient(135deg, rgba(124, 152, 133, 0.12), rgba(164, 184, 168, 0.08));
    backdrop-filter: blur(var(--blur-md));
    -webkit-backdrop-filter: blur(var(--blur-md));
    border: 1px solid var(--color-accent);
    color: var(--color-accent);
    box-shadow: var(--glass-shadow);
  }

  .btn-primary:hover {
    background: linear-gradient(135deg, rgba(124, 152, 133, 0.18), rgba(164, 184, 168, 0.12));
    border-color: var(--color-accent-light);
    color: var(--color-accent-light);
    transform: translateY(-2px);
    box-shadow: 0 12px 40px rgba(124, 152, 133, 0.15);
  }

  .btn-secondary {
    background: var(--glass-bg);
    backdrop-filter: blur(var(--blur-md));
    -webkit-backdrop-filter: blur(var(--blur-md));
    border: 1px solid var(--glass-border);
    color: var(--color-text);
    box-shadow: var(--glass-shadow);
  }

  .btn-secondary:hover {
    background: var(--glass-bg-hover);
    border-color: rgba(124, 152, 133, 0.2);
    transform: translateY(-2px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.1);
  }

  .helpful-links {
    margin-top: 1rem;
  }

  .helpful-links h3 {
    font-family: var(--font-display);
    font-size: 1.125rem;
    font-weight: 500;
    color: var(--color-text);
    margin: 0 0 1rem 0;
    letter-spacing: -0.015em;
  }

  .quick-links {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .quick-link {
    display: inline-block;
    padding: 0.5rem 1rem;
    background: rgba(124, 152, 133, 0.05);
    border: 1px solid rgba(124, 152, 133, 0.1);
    border-radius: var(--radius-sm);
    color: var(--color-text-secondary);
    text-decoration: none;
    font-family: var(--font-serif);
    font-size: 0.95rem;
    transition: all var(--transition-fast);
  }

  .quick-link:hover {
    background: rgba(124, 152, 133, 0.08);
    border-color: rgba(124, 152, 133, 0.15);
    color: var(--color-accent);
    transform: translateY(-1px);
  }

  @media (max-width: 1024px) {
    .error-content {
      grid-template-columns: 1fr;
      gap: 3rem;
      text-align: center;
    }

    .error-visual {
      height: 300px;
    }

    .error-number span {
      font-size: clamp(3rem, 10vw, 6rem);
    }
  }

  @media (max-width: 640px) {
    .error-page {
      padding: 2rem 0;
    }

    .error-content {
      gap: 2rem;
    }

    .error-visual {
      height: 200px;
    }

    .glass-1, .glass-2, .glass-3 {
      width: 60px;
      height: 40px;
    }

    .error-title {
      font-size: 2rem;
    }

    .error-description {
      font-size: 1.1rem;
    }

    .btn {
      padding: 0.875rem 1.5rem;
      font-size: 1rem;
    }

    .error-actions {
      justify-content: center;
    }

    .quick-links {
      justify-content: center;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .error-page::before,
    .floating-glass,
    .error-number span {
      animation: none;
    }

    .floating-glass {
      transform: none !important;
    }
  }
</style>
