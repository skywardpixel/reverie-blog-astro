# Prevent issues with optional dependencies on Vercel
legacy-peer-deps=true

# Use npm ci for faster, reliable builds
prefer-offline=true
audit=false

# Optimize for deployment environments
fund=false
progress=false
